class MyNode <fold text='{...}' expand='true'>{

    string get_next() <fold text='{...}' expand='true'>{
        int l = 0;
        try {
            int i = 0;
            int k = 10;
            string d = "Error is here";
        }
        catch(Exception e) {
            print(e);
        }
    }</fold>

    public void simple() <fold text='{...}' expand='true'>{
        int l = 0;
    }</fold>

}</fold>