struct SomeStruct <fold text='{...}' expand='true'>{

   int field;

   public void simple() <fold text='{...}' expand='true'>{
        int l = 0;
    }</fold>

}</fold>

enum SomeEnum <fold text='{...}' expand='true'>{
    FIRST,
    SECOND,
    THIRD
}</fold>